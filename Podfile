source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '8.0'

target 'PasswordFactory' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for PasswordFactory
  pod 'SDWebImage', '~> 3.7.6', :inhibit_warnings => true
  pod 'libextobjc', '0.4.1', :inhibit_warnings => true
  pod 'Masonry', '0.6.4', :inhibit_warnings => true
  pod 'YapDatabase', '3.0.1', :subspecs => ['Standard/Core'], :inhibit_warnings => true
  pod 'MBProgressHUD', '1.0.0', :inhibit_warnings => true
  pod 'ReactiveCocoa', '2.5',  :inhibit_warnings => true

end

target 'PasswordFactoryTests' do
    inherit! :search_paths
    # Pods for testing
end

target 'PasswordFactoryUITests' do
    inherit! :search_paths
    # Pods for testing
end
